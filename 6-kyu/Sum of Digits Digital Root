def digital_root(n):
    digits = sum([int(x) for x in str(n)])
    
    while len(str(digits)) != 1:
        digits = sum(int(i) for i in str(digits))
        
    return digits

# https://www.codewars.com/kata/541c8630095125aba6000c00/python
        
        
